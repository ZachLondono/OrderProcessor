@using Domain.Components
<Button OnClickAsync="OpenProductDrawingManager" Class="relative">
    DXFs 
    @if (DataContext?.DrawingCount != 0) {
        <div class="absolute inline-flex items-center justify-center w-6 h-6 text-xs font-bold text-white bg-red-500 border-2 border-white rounded-full -top-2 -right-2 dark:border-gray-900">@DataContext?.DrawingCount</div>
    }
</Button>
